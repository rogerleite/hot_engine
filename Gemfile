source "http://rubygems.org"

# Declare your gem's dependencies in hot_engine.gemspec.
# Bundler will treat runtime dependencies like base dependencies, and
# development dependencies will be added by default to the :development group.
gemspec

gem 'debugger'

# Register hello_engine for dummy app test
gem "hello_engine", :path => "test/dummy/vendor/engines/hello_engine"
